<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.thecocktaildb">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".TheCockTailApplication"
        android:allowBackup="true"
        android:icon="@drawable/cock"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cock"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThecockTailDB">
        <activity android:name=".ui.splash.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.details.DetailCockTailActivity" />
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>